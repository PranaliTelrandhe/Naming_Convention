name: Build C Project on Windows

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  build:
    runs-on: [self-hosted, windows]

    # This ensures all "run" commands execute inside the code folder
    defaults:
      run:
        working-directory: ./Naming_Convention

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Add Tools to PATH
        # We run this at the root level because it's a system configuration
        working-directory: .
        run: |
          "C:\msys64\mingw64\bin" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
          "C:\Program Files\CMake\bin" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
          "C:\Program Files\LLVM\bin" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
        shell: powershell

      - name: Run Semgrep Scan
        run: semgrep --config semgrep-naming-rules.yml
        continue-on-error: true

      - name: Run Clang Format
        run: |
          $files = Get-ChildItem -Path . -Include *.c,*.h -Recurse
          if ($files) {
              foreach ($file in $files) { clang-format -i $file.FullName }
          }
        shell: powershell

      - name: Generate build files with CMake
        run: |
          # Clean old build to fix the "Different Directory" error
          if (Test-Path build) { Remove-Item -Recurse -Force build }

          cmake -S . -B build -G "MinGW Makefiles" `
            -DCMAKE_C_COMPILER=gcc `
            -DCMAKE_CXX_COMPILER=g++
        shell: powershell

      - name: Build project
        run: cmake --build build
        shell: powershell

      - name: Run Clang-Tidy
        run: |
          $files = Get-ChildItem -Path . -Include *.c,*.h -Recurse
          foreach ($file in $files) {
              clang-tidy $($file.FullName) -p build -checks="clang-analyzer-*,bugprone-*"
          }
        shell: powershell

      - name: Run executable
        run: |
          if (Test-Path "build/hello.exe") {
              ./build/hello.exe
          } else {
              Write-Host "Executable not found in build folder. Checking root..."
              ./hello.exe
          }
        shell: powershell
